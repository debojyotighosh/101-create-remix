const BooksWaitlist = () => {
  return (
    <div>
      <h1>Book wishlist</h1>
      <p>
        <span className="px-1 border border-red-400 bg-red-950 rounded">books_</span> folder
        ensures that the layout is not nested, while still keeping the URL nesting pattern
      </p>
    </div>
  )
}

export default BooksWaitlist