// `books_` folder ensures that the layout is not nested while still keeping the URL nesting pattern

const BooksWaitlist = () => {
  return (
    <div>
      <h1>Book wishlist</h1>
    </div>
  )
}

export default BooksWaitlist